{% extends 'base.html.twig' %}
{% block title %} {{ 'title.achievements' | trans }} {% endblock %}
{% block body %}
<div class ="container">
    <div class="row section-content-2">

        <div class="form-section">
            <div class="col-sm-8 col-sm-offset-2">
                    <div class="kc-elm kc-title-wrap  left dark">
                        <div class="title-wrapper  margin-bottom-30">
                            <h3 class="kc_title">{{ 'achievements.header' | trans | raw }}</h3>
                        </div>
                    </div>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    {{ content | raw }}
                </div>
            </div>
        </div>

    </div>
</div>
{% endblock %}
